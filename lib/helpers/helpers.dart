// this file contains mostly variables to be reused and stored here for easy management

import 'dart:ui';

import 'package:flutter/material.dart';

// primary colors
final Color primaryColor = Color(0xff00acc1);
final Color primaryColorLight = Color(0xff5ddef4);
final Color primaryColorDark = Color(0xff007c91);

// secondary colors
final Color secondaryColor = Color(0xffe91e63);
final Color secondaryColorLight = Color(0xffff6090);
final Color secondaryColorDark = Color(0xffb0003a);

